<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.mapper.oracle.faq">
	<select id="selectFaq" resultType="FaqVo">
		select * from fnq order by fnq_No desc
	</select>
	
	<insert id="WriteFaq" parameterType="faqVo">
		<selectKey keyProperty="fnqNo" resultType="int" order="BEFORE">
			select fnq_seq.nextval from dual
		</selectKey>
		insert into fnq
		values(#{fnqNo},#{category},#{question},#{answer})
	</insert>
	
	<select id="selectByNo" parameterType="int" resultType="faqVo">
		select * from fnq
		where fnq_no=#{fnqNo}
	</select>
	
	<update id="editFaq" parameterType="faqVo">
		update fnq
		set category = #{category}, question = #{question}, answer = #{answer}
		where fnq_no = #{fnqNo}
	</update>
	
	<delete id="deleteFaq" parameterType="int">
		delete fnq
		where fnq_no = #{fnqNo}
	</delete>
</mapper>



