<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.will.team4final.notice">
	<insert id="insertNotice" parameterType="noticeVo">
		<selectKey keyProperty="noticeNo" resultType="int" order="BEFORE">
			select notice_seq.nextval from dual
		</selectKey>
		insert into notice(notice_no, title, type, content, filename, filesize, originfilename)
		values(#{noticeNo}, #{title}, #{type}, #{content}, #{filename}, #{filesize}, #{originfilename})
	</insert>
	
	<select id="selectAllNotice" resultType="noticeVo" parameterType="searchVo">
		select * 
		from 
		(
			select rownum as RNUM, A.*
			from(
				select notice_no, title, type, regdate from notice
				<if test="searchKeyword!=null and searchKeyword != ''">
					where ${searchCondition} like '%'
							|| #{searchKeyword} || '%'
				</if>
				order by notice_no desc
			)A
		)
		<![CDATA[ 
		where RNUM>#{firstRecordIndex}
		  and RNUM<=#{firstRecordIndex }+ #{recordCountPerPage}]]>
	</select>
	
	<select id="totalNotice" parameterType="searchVo" resultType="int">
		select count(*) from notice
		<if test="searchKeyword!=null and searchKeyword != ''">
			where ${searchCondition} like '%'
				|| #{searchKeyword} || '%'
		</if>
	</select>
	
	<select id="selectNoticeByNo" resultType="noticeVo" parameterType="int">
		select * from notice where notice_no = #{noticeNo}
	</select>
	
</mapper>









