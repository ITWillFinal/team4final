<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.will.team4final.payment">

	<insert id="insertPayment" parameterType="paymentVo" >
		<selectKey keyProperty="paymentCode" resultType="String" order="BEFORE">
			select payment_seq.nextval from dual
		</selectKey>
			insert into payment(PAYMENT_CODE, PRODUCT_NAME, PRICE, START_DAY, END_DAY, C_MEMBER_CODE  )
			values(paymentCode, productName, price, startDay, endDay, cMemberCode)
	</insert>
	
</mapper>









