<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.will.team4final.payment">

	<insert id="insertPayment" parameterType="paymentVo" >
		<selectKey keyProperty="paymentCode" resultType="String" order="BEFORE">
			select payment_seq.nextval from dual
		</selectKey>
			insert into payment(PAYMENT_CODE, PRODUCT_NAME, PRICE, C_MEMBER_CODE  )
			values(#{paymentCode}, #{productName}, #{price}, #{cMemberCode})
	</insert>
	
	<select id="selectTodayPayment" resultType="String">
		select sum(price) from payment where regdate = to_char(sysdate)
	</select>
	
	<select id="selectPayment" resultType="map" parameterType="searchVo">
		select * 
		from
		(
			select rownum as RNUM, A.*
			from(
				select *
				from v_payment
				<if test="searchKeyword!=null and searchKeyword != ''">
					where ${searchCondition} like '%'
							|| #{searchKeyword} || '%'
				</if>
				order by regdate desc
			)A
		)
		<![CDATA[ 
		where RNUM>#{firstRecordIndex}
		  and RNUM<=#{firstRecordIndex }+ #{recordCountPerPage}]]>
	</select>
	
	<select id="selectTotalRecord" parameterType="searchVo" resultType="int">
		select count(*) from v_payment
		<if test="searchKeyword!=null and searchKeyword != ''">
			where ${searchCondition} like '%'
				|| #{searchKeyword} || '%'
		</if>
	</select>
	<select id="selectPamentForView" resultType="map">
		select * from v_payment order by start_date desc
	</select>
</mapper>









