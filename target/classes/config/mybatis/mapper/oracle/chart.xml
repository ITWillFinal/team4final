<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.mapper.oracle.chart">
 	<select id="sumByMonth" parameterType="string" resultType="int">
		select sum(price)as sum from payment
		where substr(regdate, 4, 2) = #{month}
	</select>
	
	<select id="countByMember" parameterType="string" resultType="int">
		select count(*)as count from member
		where substr(regdate, 4, 2) = #{month}
	</select>
	
	<select id="countByMemberC" parameterType="string" resultType="int">
		select count(*)as count from company_member
		where substr(c_joindate,4, 2) = #{month}
	</select>
	
</mapper>









